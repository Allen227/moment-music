.app-header {
  position: relative;
  height: 45px;
  color: var(--mainFontColor);
  background: var(--mainFgColor);
  .header-bg {
    position: absolute;
    z-index: -1;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    color: inherit;
    background: inherit;
  }
  .header-container {
    display: flex;
    align-items: center;
    justify-content: space-between;
    min-width: var(--headerMinWidth);
    padding: 0 24px;
    height: 45px;
    line-height: 45px;
    margin: 0 auto;
    border-bottom: 1px solid var(--borderColor);
    color: inherit;
    background: inherit;
    .app-title {
      cursor: pointer;
      font-size: 18px;
      color: inherit;
      background: inherit;
    }
    .menu-list {
      display: flex;
      justify-content: space-between;
      list-style: none;
      cursor: pointer;
      .menu-item {
        padding: 0 24px;
        margin-right: var(--fontMiddleSize);
        text-decoration: none;
        &:hover {
          color: var(--mainColor);
        }
      }
      .active {
        background: var(--mainColor);
        color: white;
        &:hover {
          color: white;
        }
      }
    }
  }
}